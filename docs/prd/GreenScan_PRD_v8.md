# GreenScan PRD Agent Directive v8

## 문서 목적과 우선순위

이 문서는 GreenScan의 API 명세, DB 설계, 백엔드 모듈, 프론트엔드 흐름, 테스트와 구현 계획을 만들거나 검토하는 에이전트의 최상위 지시문이다.

- 기준 문서: GreenScan PRD Agent Directive v7 (`GreenScan_PRD_v7.md`, 이 저장소에는 v8로 대체됨)
- 이번 버전: v8
- v8의 핵심 변경: 디자인팀 시안 검토 후 PM이 의도적으로 결정한 이탈 사항을 문서에 정식 반영한다. **로그인/계정 도입**(v7의 "로그인 없는 서비스" 원칙 폐기), 홈·지도·추천·저장·마이페이지 하단 탭 구조 추가, 카카오맵 연동(주소 검색 기반 지역 판단), 결과 화면의 등급·긴급도 배지와 러프 견적 범위 텍스트를 조건부(비확정 고지 필수)로 허용한다.
- v7의 물리 계산 원칙(U값/HDD 계산식, 대표 공간 1개 기준, 사진 AI는 후보 분류기라는 경계, 건물 전체 진단 금지, 상가 제외)은 **그대로 유지**한다. 바뀐 것은 계정·이력·위치 기반 부가 기능이지, 진단 계산의 정확성·표현 원칙이 아니다.
- 이 문서의 근거가 된 논의 기록은 `docs/prd-v7-deviations.md`(작업 브랜치 `feature/fe-mockup`)에 남아 있다. 세부 조건이나 "왜 이렇게 정했는지"가 더 필요하면 그 문서를 참고할 것.

에이전트는 아래 원칙을 반드시 따른다.

1. 본 문서의 v8 결정이 v7의 무상태·비로그인 요구보다 우선한다.
2. 바뀌지 않은 v7 핵심 범위는 유지한다. 대표 공간 1개, 지원 건물, U값과 HDD 계산, 사용자 확정, 결과 표현은 유지 대상이다. 단, **원본 사진은 여전히 서버에 저장하지 않는다.** 진단 이력은 이제 로그인한 계정에 한해 저장한다(5장 참고). 저장 방식(스키마)은 BE가 별도로 확정한다.
3. 기준이 모호할 때 그럴듯한 수치나 정책을 임의로 확정하지 않는다. 필요한 값은 정책 확정 필요로 분리하고 DB에서 버전 관리할 수 있게 설계한다. 이번 버전에서 새로 열린 정책 확정 필요 항목(로그인 방식, 등급 산정 로직, 견적 근거값, 지도 공유 범위 등)은 문서 곳곳에 명시했다.
4. 사진 AI는 후보 분류기다. AI 원본 응답이나 모델 신뢰도만으로 계산값을 확정하면 안 된다.
5. 실제 U값, 단열재 상태, 구조 안전성, 누수 원인, 건물 전체 에너지 사용량을 사진만으로 진단하는 것처럼 구현하거나 표현하지 않는다.
6. 공식 인증, 건물 전체 진단, 상가 지원은 새 기능으로 추가하지 않는다. **비용·견적은 근거 데이터 없는 러프 범위 텍스트로만 표시할 수 있으며, 반드시 "참고용 추정치, 실제 시공비는 현장 확인 후 별도 안내" 같은 비확정 고지를 동반한다.** 정밀 단가 모델(단가 테이블 등)은 이번 범위가 아니다.
7. 계산은 서버 계산 엔진이 수행한다. 프론트엔드는 계산식을 중복 구현하지 않는다.
8. MVP에 GPU 서버, 자체 모델 학습, YOLO, 벡터 DB, LangChain, Redis 또는 Celery를 기본 전제로 넣지 않는다.
9. **등급·우선순위 표시(LV.n, 긴급/주의/권장 등)를 쓸 때는 반드시 "참고용 추정치이며 실제와 다를 수 있다"는 고지를 함께 표시한다.** 고지 없이 단정적인 등급만 단독으로 노출하지 않는다. 등급 산정 로직(임계값, 계산식)은 이 문서에서 확정하지 않으며 BE-C가 정책으로 확정하기 전까지는 프론트/디자인 어디에도 실제 로직인 것처럼 하드코딩하지 않는다.
10. **사용자 간 데이터 공유(지도에서 다른 사용자의 진단 정보를 보여주는 등)는 명시적 동의 없이 노출하지 않는다.** 공유 범위, 데이터 항목, 동의 UX는 이번 버전에서 확정하지 않고 보류한다(6.3절 참고) — 설계·구현 착수 전 별도 정책 확정이 선행되어야 한다.

---

## 1. 제품 정의

### 1.1 한 줄 정의

GreenScan은 노후 단독·다가구주택 및 아파트의 대표 공간 1개를 대상으로, 사용자가 입력한 공간 정보와 창호·벽체 사진의 AI 후보 분류를 결합해 추정 연간 열손실과 그린리모델링 개선 우선순위를 제시하는 전문가 진단 이전 단계의 1차 사전진단 서비스다. v8부터는 로그인한 사용자에 한해 진단 기록을 계정에 보관하고, 홈 화면과 지도를 통해 접근할 수 있다.

### 1.2 사용자가 얻는 것

- 대표 공간 1개 기준의 창호와 외기 접촉 벽체 연간 전도 열손실 추정치
- 창호 개선, 벽체 개선, 복합 개선 등 2~3개 시나리오의 추정 감소량과 절감률
- 추정 열손실 감소량이 큰 순서의 개선 우선순위, 참고용 긴급도 배지(9번 원칙에 따른 고지 동반)
- 참고용 에너지 효율 레벨 표시(9번 원칙에 따른 고지 동반)
- 참고용 러프 개선 비용 범위(6번 원칙에 따른 고지 동반)
- 사진상 이상 흔적 후보에 대한 별도 현장 점검 권장 안내
- 결과에 사용된 사용자 입력, AI 후보, 기준 데이터 버전, 계산 근거
- (로그인 시) 과거 진단 기록 조회, 홈/마이페이지에서 최근 분석 이력 확인

### 1.3 절대 표현 금지

- 건물 전체 에너지 사용량 또는 고지서 사용량을 정확히 예측, 보정, 검증했다.
- 공식 에너지 등급 또는 법적 적합성 판정을 제공한다.
- 사진 AI가 실제 U값, Low-E 여부, 단열재 유무, 구조 안전성, 균열이나 누수 원인을 확정했다.
- 사진상 이상 흔적 후보 때문에 열손실 수치가 자동으로 증가했다.
- 한 방 결과가 아파트 세대 전체 또는 건물 전체 결과다.
- **고지 없이** 단정적인 위험도·등급·긴급도를 표시한다(고지를 동반하면 9번 원칙에 따라 허용).
- **정밀 근거 없이** 확정 견적 금액을 표시한다(러프 범위 텍스트 + 비확정 고지는 6번 원칙에 따라 허용).
- 동의하지 않은 사용자의 진단 정보를 다른 사용자에게 노출한다.

결과 이름은 GreenScan 추정 연간 열손실 분석 또는 이와 동등하게 비공식 추정치임을 드러내는 표현을 쓴다. 결과 화면에는 이 방 기준을 명시한다.

---

## 2. v7에서 v8로 바뀐 MVP 범위

### 2.1 기본 진단 흐름 (변경 없음, 진입 경로만 추가)

1. (신규) 사용자가 로그인한다 — 로그인 방식은 5장 참고, 미확정.
2. (신규) 홈 화면에서 "AI 진단 시작하기"를 누른다.
3. 사용자가 건물 유형과 대표 공간을 선택한다.
4. 사용자가 대표 공간의 가로, 세로, 높이, 창호 합산면적, 외기 접촉 벽체 합산면적을 입력한다.
5. 사용자가 창호 사진과 벽체 사진을 업로드한다.
6. FastAPI가 사진을 검증한 뒤 Vision API에 일회성으로 전달하고, 구조화된 후보값을 수신한다.
7. 원본 사진은 분석 요청이 끝난 직후 서버의 메모리 또는 임시 파일 영역에서 폐기한다. **(변경 없음 — 로그인 도입과 무관하게 원본 사진은 여전히 저장하지 않는다.)**
8. 사용자가 AI 후보를 확인, 수정, 확정한다.
9. 사용자가 MVP 지원 지역 목록에서 지역을 선택하거나, (신규) 카카오맵 주소 검색으로 지역을 자동 판별한다 — 6장 참고.
10. 서버가 기준값과 물리식으로 계산한다.
11. 사용자가 추정 연간 열손실, 개선 시나리오, 등급·긴급도·러프 견적(고지 동반), 사진상 이상 흔적 안내를 확인한다.
12. (신규) 로그인 상태라면 이 진단 결과가 계정에 귀속되어 저장되고, 마이페이지/홈에서 다시 조회할 수 있다. 비로그인 이용 가능 여부는 미확정(5장 참고).

### 2.2 앱 구조 — 하단 탭 (신규)

디자인팀 시안 검토 후 추가된 구조. 하단 탭 5개:

| 탭 | 역할 | 상태 |
|---|---|---|
| 홈 | "AI 진단 시작하기" 진입점 + 최근 분석한 방 목록 | 이번 버전에 포함, 목록은 계정 기반 |
| 지도 | 카카오맵 연동 — 6장 참고 | 주소 검색·지역 판별만 이번 범위. 주변 사용자 공유는 보류 |
| 추천 | 내용 미정 | placeholder, 정책 확정 필요 |
| 저장 | 즐겨찾기/관심 건물 저장 (design 시안의 하트 아이콘) | 내용 미정, 정책 확정 필요 |
| 마이페이지 | 프로필, 진단 기록, 계정 설정, 로그아웃 | 이번 버전에 포함 |

이 탭 구조는 v7의 "단일 진단 위저드 흐름"을 감싸는 앱 셸이다. 진단 흐름(공간입력→사진→AI확인→계산→결과) 자체의 화면 순서와 계산 로직은 바뀌지 않는다.

### 2.3 LiDAR의 지위 (변경 없음)

- LiDAR, RoomPlan, iOS 앱, 공간 스캔, 스캔 프레임 자동 추출은 이번 MVP 구현 범위가 아니다.
- 디자인 시안의 "스캔" 버튼은 라이다가 아니라 단순 카메라 촬영 버튼임을 확인함 — 혼동하지 않는다.
- 향후 확장을 고려해 입력 출처 필드는 둘 수 있다. 현재 구현값은 manual과 user_corrected만 허용한다.

### 2.4 사진 AI의 역할 경계 (변경 없음)

v7과 동일. 창호 사진 → 창호 유형/품질 후보, 벽체 사진 → 이상 흔적 후보(있음/없음/판단불가). 계산에 쓰는 값은 사용자 확정값만.

벽체 이상 흔적의 `reason_summary` 예시에 "곰팡이로 추정되는 흔적 → 습도가 높을 가능성" 같은 문구를 쓸 수 있다 — 다만 이것도 원인을 확정하는 것이 아니라 현장 점검 권장 안내 수준을 유지해야 하며, U값·면적·열손실 계산에는 영향을 주지 않는다.

---

## 3. 대상 및 진단 범위 (변경 없음)

v7과 동일. 단독·다가구주택/아파트, 대표 공간 1개. 상가, 공용부, 세대 전체 진단은 여전히 제외 대상이다. **디자인 시안에 있던 1,000㎡ 이상 상업·업무·공공 건물 지원은 이번 버전에도 반영하지 않는다.**

---

## 4. 권장 기술 아키텍처

v7 표에 아래 항목을 추가한다.

| 계층 | 권장 구성 | 책임 |
|---|---|---|
| 웹 클라이언트 | React, TypeScript, Vite, Vercel | 입력, 사진 촬영 또는 선택, 업로드 상태, AI 후보 확인과 수정, 결과 표시, (신규) 로그인, 지도, 마이페이지 |
| 지도 연동 (신규) | 카카오맵 JavaScript SDK | 주소 검색, 좌표→지원 지역 매핑, 지도 표시 |
| 인증 (신규) | 방식 미확정 — 이메일/비밀번호 또는 소셜 로그인 | 로그인, 세션/토큰 발급, 계정별 진단 이력 접근 제어 |
| API 서버 | Python, FastAPI, Uvicorn | 입력 검증, 사진의 일회성 Vision API 전달, 계산 요청, 결과 반환, (신규) 인증, 이력 CRUD |
| 관계형 DB | PostgreSQL | U값, HDD, 지원 지역, 기준 버전과 출처 등 정적 기준 데이터 + (신규) 사용자 계정, 진단 이력 |

그 외(AI, 배포, GPU/API 키 정책)는 v7과 동일.

### 4.1 카카오맵 연동 범위 (신규, 정책 확정 필요 다수)

- **이번 범위**: 사용자가 주소를 검색하면 카카오맵(로컬/주소 검색 API)으로 좌표를 얻고, 그 좌표가 MVP 지원 지역(서울/김포 등, 7.3절) 안에 있는지 판별해 HDD 조회에 사용한다. 지도에는 "내가 최근 검색한 지역" 핀을 표시할 수 있다.
- **보류 (이번 범위 아님)**: "동의한 주변 사용자의 건물 정보(건축연도 등)를 지도에 표시"하는 기능은 PM이 명시적으로 보류했다. 아래가 모두 확정되기 전까지 설계·구현에 착수하지 않는다.
  - 정확히 어떤 데이터 항목을 노출할지 (건축연도만? 등급까지? 정확한 주소 노출 여부?)
  - "동의"의 UX — 언제, 어떻게 동의를 받고 철회는 어떻게 할지
  - "주변"의 정의 — 반경, 위치 정밀도(정확한 건물 좌표 vs 흐려진 좌표)
  - 이 데이터가 개인정보에 해당하는지, 비식별화가 필요한지
- 카카오맵 API 키는 FastAPI 프록시를 통해서만 쓰는지, 프론트에 공개 키를 직접 노출해도 되는지도 확인 필요(카카오맵 JS SDK는 통상 도메인 제한된 공개 키를 프론트에 둔다 — 이번 프로젝트에 적용 가능한지 검토).

---

## 5. 계정과 로그인 (신규 — v7의 "로그인 없는 서비스" 원칙 폐기)

v7 5.1절 "로그인 없는 서비스다. MVP에서는 서버 측 게스트 세션, 진단 이력, TTL 자동 삭제 작업을 구현하지 않는다"는 **더 이상 유효하지 않다.**

- 로그인 기능을 실제로 구현한다. **로그인 방식(이메일/비밀번호, 소셜 로그인 및 어떤 provider인지)은 이 문서에서 확정하지 않는다 — 정책 확정 필요.**
- 비로그인 상태에서도 진단 자체(계산까지)를 이용할 수 있는지, 아니면 로그인이 필수인지도 **미확정**. 확정 전까지는 "로그인 후 이용"을 기본 가정으로 설계하되 화면 흐름에 비로그인 우회 가능성을 완전히 막아두지 않는다.
- 로그인한 사용자의 진단 결과(입력값, 확정값, 계산 결과 — **원본 사진은 제외**)는 계정에 귀속되어 서버에 저장된다. 이는 v7 9.2절의 "PostgreSQL에는 사용자 진단 이력을 저장하지 않는다"는 규칙을 대체한다.
- 저장 스키마(사용자 테이블, 진단 이력 테이블, 보관 기간)는 BE가 별도로 설계 중이며 이 문서가 세부 스키마를 정하지 않는다.
- **원본 사진 무저장 원칙은 로그인 도입과 무관하게 그대로 유지한다.** 계정에 저장되는 것은 사용자 확정값과 계산 결과이지 사진 원본이 아니다.
- 마이페이지(2.2절)에서 계정 정보 수정, 진단 기록 조회, "진단 기록 저장 끄기" 같은 옵트아웃, 로그아웃을 제공한다.

---

## 6. 지도와 추천 탭 상세

### 6.1 지도 탭 — 확정 범위

- 카카오맵으로 주소를 검색해 지원 지역 여부를 자동 판별 (4.1절)
- 내가 최근 검색/진단한 지역을 지도 핀으로 표시

### 6.2 지도 탭 — 보류 범위

동의한 주변 사용자 정보 공유 기능은 4.1절에 정리된 미확정 항목이 모두 정책으로 확정되기 전까지 **설계도, 구현도 시작하지 않는다.** API 명세나 DB 스키마에도 이 기능을 위한 테이블/필드를 임의로 미리 만들지 않는다 — 확정된 뒤에 그때 추가한다.

### 6.3 추천 탭

내용 미정. 디자인 시안의 "추천솔루션" 아이콘과 관련이 있을 것으로 추정되나, 무엇을 추천하는지(개선 시공 옵션? 유사 사례? 협력사?) PM이 아직 정하지 않았다. **정책 확정 필요 — 이 문서는 탭의 존재만 기록하고 내용은 비워둔다.**

---

## 7. 임시 진단 흐름과 사용자 입력 (v7 5장 계승, 로그인 반영)

### 7.1 진단 상태

- v7과 달리 이제 로그인 기반 서비스다(5장). 로그인한 사용자의 입력값, AI 후보값, 사용자 확정값, 계산 결과는 계정에 귀속되어 저장될 수 있다.
- 비로그인 이용 가능 여부가 확정되기 전까지, 화면 상태 관리는 v7과 동일하게 React 임시 상태 기반으로 두고 로그인 시점에 서버 저장을 트리거하는 방식을 기본으로 가정한다.
- FastAPI의 분석 API(사진)와 계산 API는 v7과 동일하게 요청-즉시응답 방식을 유지한다. **원본 사진은 여전히 저장하지 않는다.**

### 7.2 필수 입력 (변경 없음)

v7 5.2절과 동일 — 건물 유형, 대표 공간, 공간 치수, 창호/벽체 면적, 준공연도 구간, 지원 지역, 선택 고지서. 벽체 순면적 검증 규칙도 동일.

### 7.3 입력 출처 (변경 없음)

v7과 동일 — manual, user_corrected, (예약값) lidar.

---

## 8. Vision AI 상세 요구 (변경 없음)

v7 6장과 동일. 사진 카테고리, 구조화 응답 계약(`assessment_status`, `photo_quality`, `component_type`, `window_type_candidate`, `visible_anomaly_candidate`, `needs_user_confirmation`, `reason_summary`), 사용자 확정 규칙 모두 유지. `visible_anomaly_candidate`의 `reason_summary` 예시 문구만 2.4절 언급대로 곰팡이/습도 케이스를 포함할 수 있다.

---

## 9. 기준 데이터와 계산 규칙 (물리 계산은 변경 없음)

v7 7장과 동일 — 현재 U값/개선 목표 U값 분리, 고시·별표 데이터 관리, MVP 지원 지역과 HDD, 열손실 계산식(`annual_heat_loss_kwh = u_value × area_m2 × 24 × hdd ÷ 1000`).

### 9.1 결과와 시나리오 (표시 방식만 변경)

- 기준선, 시나리오, 우선순위, 절감률 산출 로직은 v7과 동일.
- **추가**: 각 시나리오에 참고용 긴급도 배지(긴급/주의/권장 등)와 러프 견적 범위 텍스트를 표시할 수 있다 — 원칙 9, 6 참고. 반드시 "참고용 추정치, 실제와 다를 수 있음" 및 "실제 시공비는 현장 확인 후 별도 안내" 고지를 동반한다.
- **추가**: 결과 상단에 참고용 "에너지 효율 레벨"(예: LV.3)을 표시할 수 있다 — 산정 로직은 정책 확정 필요, 고지 필수.
- 절감률의 분모는 여전히 기준선 열손실량이며, 건물 전체 에너지 사용량 절감률로 표현하지 않는다.

---

## 10. 상태 전이와 오류 처리 (변경 없음)

v7 8장과 동일한 상태 전이(`draft → input_pending → photo_pending → ai_analyzing → confirmation_required → calculation_ready → completed`)와 오류 분류표를 유지한다. 로그인/비로그인 분기가 상태 전이에 미치는 영향은 5장 정책이 확정된 뒤 별도로 반영한다.

---

## 11. API와 DB 설계 지시 (계산/기준데이터 부분 변경 없음, 계정 영역 신규)

v7 9장의 API 책임 분리, 사진의 일회성 처리, 계산 API 규칙은 동일하게 유지한다. 아래를 추가한다.

- (신규) 인증 API — 로그인, 세션/토큰 발급. 방식 미확정.
- (신규) 진단 이력 조회/저장 API — 계정에 귀속된 과거 진단 결과 CRUD. **원본 사진은 이 이력에 포함하지 않는다.**
- (신규) 주소 검색/지역 판별 API — 카카오맵 좌표를 받아 지원 지역 여부와 HDD 조회 키를 반환.
- PostgreSQL은 이제 기준 데이터뿐 아니라 사용자 계정과 진단 이력도 저장한다 — v7 9.2절의 "진단 이력을 저장하지 않는다"는 문구는 폐기한다. **사진 파일/사진 메타데이터/AI 원본 응답은 여전히 저장하지 않는다.**

---

## 12. 백엔드 역할 분담과 통합 (변경 없음, 신규 영역만 추가)

v7 10장의 BE-A/BE-B/BE-C/통합담당 분담은 유지한다. 인증·이력·지도 연동은 BE-A 책임 범위로 편입하거나 필요 시 팀과 협의해 역할을 나눈다(이 문서가 담당자를 강제하지 않는다).

---

## 13. MVP 우선순위 (v7 대비 변경)

### Must (v7 목록 + 아래 추가)

- 로그인 기능 (방식 미확정이나 Must로 반영 — PM 결정)
- 홈 화면(AI 진단 시작하기 진입점 + 최근 분석한 방)
- 마이페이지(진단 기록 조회, 계정 설정, 로그아웃)
- 지도 탭 — 카카오맵 주소 검색 기반 지원 지역/HDD 자동 판별
- 결과 화면의 등급/긴급도 배지 + 러프 견적 범위 텍스트(고지 동반)
- (기존 v7 Must 전부 유지 — 계산 로직, 사진 AI 흐름, 시나리오 표시 등)

### Should

- v7 Should 항목 유지 (고지서 참고 비교, 촬영 가이드 고도화, Vision 재시도 UI, 결과 근거 상세보기)
- 저장(즐겨찾기) 탭 내용 확정 및 구현

### Could

- v7 Could 항목 유지 (LiDAR/RoomPlan, 기준물 사진 면적 추정, 공공 원단위 통계 비교)
- 추천 탭 내용 확정 및 구현
- 지도 내 "동의한 주변 사용자 정보 공유" — **정책 확정 전까지 설계 착수 금지** (6.2절)

### 제외 (v7 대비 변경)

- ~~계정과 로그인, 장기 이력~~ → **제외 목록에서 삭제. 이번 버전부터 Must.**
- 포토그래메트리
- 자체 Vision 모델 학습, GPU 모델 서빙
- 창호와 벽체 객체별 영구 추적
- Low-E AI 자동 판별
- 상가, 1,000㎡ 이상 상업·업무·공공 건물 지원
- 12개월 고지서 보정
- 시공비, 견적, 시공업체 연결의 **정밀 견적/견적서 발급 기능** (러프 범위 텍스트 표시는 제외 대상 아님 — 원칙 6 참고)
- 사용자 간 데이터 공유의 **비동의 노출** (동의 기반 공유 자체는 정책 확정 후 Could로 검토 가능)

---

## 14. 통합 테스트와 완료 기준 (v7 유지 + 추가 필요)

v7 12장의 11개 시나리오는 계산 로직이 바뀌지 않았으므로 그대로 유효하다. 아래는 v8에서 추가로 필요한 시나리오이며, **이 문서는 구체적 테스트 케이스까지는 작성하지 않는다** — 로그인/지도 정책이 확정된 뒤 별도로 작성한다.

- 로그인/로그아웃, 진단 이력이 계정에 정상 귀속되는지
- 비로그인 상태에서의 동작(이용 가능 여부 확정 후)
- 카카오맵 주소 검색 → 지원 지역 판별이 정확한지, 미지원 지역일 때의 처리
- 등급/견적 표시에 고지 문구가 항상 동반되는지
- 원본 사진이 로그인 여부와 무관하게 여전히 저장되지 않는지 (v7 통합테스트 9번의 연장)

---

## 15. 에이전트 산출물 요구 (변경 없음)

v7 13장과 동일한 산출물 요구를 유지한다. 다만 6.2절(지도 사용자 공유)처럼 이 문서가 명시적으로 "보류"라고 표시한 기능은 에이전트가 API/DB 설계 산출물에 임의로 포함하지 않는다.

---

## 16. 최종 제품 메시지 (갱신)

현재 MVP 메시지는 다음으로 고정한다.

사진 기반 AI 건축요소 후보 분류와 사용자 입력, 검증된 지원 지역의 공공 기준값과 물리식을 결합해 노후주택 대표 공간의 추정 연간 열손실과 그린리모델링 개선 우선순위를 제시하는 사전진단 서비스. 로그인한 사용자는 진단 기록을 계정에 보관하고 지도에서 다시 확인할 수 있다.

LiDAR는 현재 MVP의 전제가 아니다. 사진 AI는 측정이나 인증 도구가 아니라, 사용자가 확인하고 수정할 수 있는 건축요소 상태 후보를 빠르게 제공하는 보조 판단 도구다. 결과 화면의 등급·긴급도·견적 표시는 가독성을 위한 참고용 요소이며, 공식 진단이나 확정 견적을 대체하지 않는다는 점을 항상 고지한다.
